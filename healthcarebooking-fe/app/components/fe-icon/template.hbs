{{!-- <i data-feather="{{value}}"></i> --}}

<svg width="{{width}}" height="{{height}}" fill="none"
    stroke="currentColor"
    stroke-width="{{stroke-width}}"
    stroke-linecap="round"
    stroke-linejoin="round">
    <use xlink:href="assets/feather-sprite.svg#{{name}}" />
</svg>